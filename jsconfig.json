{
  "compilerOptions": {
    "target": "es5",
    "module": "esnext",
    "baseUrl": "./", // Re-añadido para que el alias @/* funcione
    "ignoreDeprecations": "6.0", // Incluido para silenciar advertencias como lo solicitaste
    "moduleResolution": "node", // Corregido el valor estándar
    "paths": {
      "@/*": [
        "src/*"
      ]
    },
    "lib": [
      "esnext",
      "dom",
      "dom.iterable",
      "scripthost"
    ]
  },
  "include": [
    "src/**/*.js",   // Para proyectos JavaScript
    "src/**/*.jsx",
    "src/**/*.vue"
  ],
  "exclude": [
    "node_modules" // Asegura que la compilación sea rápida
  ]
}